[project]
name = "ark-sdk-builder"
version = "0.1.51"
requires-python = ">= 3.12"
dependencies = [
    "build",
    "pytest",
    "pyyaml",
    "kubernetes",
    "pydantic",
    "urllib3>=2.6.0",
]

[tool.setuptools]
py-modules = []
